# サーバー・インフラ

**担当：森島**

## 構成概要

| 要素 | 内容 |
|------|------|
| サーバー機器 | Raspberry Pi 3 Model B（松本研究室所有） |
| OS | Raspberry Pi OS |
| コンテナ管理 | Docker |
| 外部公開 | Cloudflare Tunnel |
| ドメイン | お名前.com で取得（`practice.circlemanage.jp`） |

---

## デプロイ・ワークフロー

```
ローカル開発・テスト
      ↓
   git push（GitHub）
      ↓
  Raspberry Pi 上で git pull
      ↓
  Docker コンテナとしてデプロイ
```

- **開発体制**：阿部・南によるフロントエンド/バックエンドのコーディング
- コードは GitHub で集約管理し、ラズパイ上で `git pull` して反映

---

## Cloudflare Tunnel による外部公開

ポート開放を行わず、Cloudflare Tunnel 経由でインターネットに公開しています。

**選定理由：**
- ルーターのポート開放が不要でセキュリティリスクを低減
- Cloudflare の DDoS 対策・SSL 証明書管理を無料で利用可能
- 自宅ネットワークの IP アドレスを外部に露出しない

---

## 開発・デプロイの経緯

Vercel / Render でのプロトタイプ開発から、Raspberry Pi を用いたセルフホスト環境での実用化まで、以下のステップを経て構築しました。

### 1. インフラ構築の経緯

- **選定理由**：最小単位での初期リリースを目指し、研究室の資産（Raspberry Pi）を活用してインフラコストを完全に無料化
- 初期は Vercel（フロントエンド）/ Render（バックエンド）で動作確認。実用化にあたりラズパイへ移行

### 2. 直面した課題と解決策

#### ① 環境変数の管理

- **課題**：Vercel からラズパイへ移行した際、バックエンドが正常に動作しない問題が発生
- **解決**：クラウド環境と同様に、サーバー側でも `.env` ファイルによる API キー管理が必要であることを確認し、環境変数の注入を徹底

#### ② API クォータ削減のための DB キャッシュ実装

- **課題**：外部 API（NAVITIME・Google Maps）の呼び出し回数に制限があり、リクエストのたびに計算するのが非効率だった
- **解決**：PostgreSQL を導入し、一度取得した駅間の所要時間・座標データを DB に保存するキャッシュ機構を実装。API 消費量を大幅に削減しつつレスポンスを高速化（詳細は [backend.md](./backend.md) 参照）

#### ③ Google OAuth の設定

- **課題**：初期実装時に Google Cloud Console の設定ミスにより「安全ではないページ」警告が表示される問題が発生
- **解決**：OAuth クライアントの承認済み JavaScript 生成元・リダイレクト URI を正確に定義し直してセキュリティ警告を解消

---

## サーバー設立の原理（自分用整理）

> 参考記事：https://zenn.dev/togetine/articles/6c9db2db7a9dd9  
> 記事を元にラズパイのサーバー化を実施。仕組みを初心者向けに整理したもの。

---

### 一言でいうと

> **「Cloudflare を盾・中継役にして、家のラズパイを安全にインターネットに公開した」**

---

### 登場人物の整理

| モノ | 正体 | 例え |
|------|------|------|
| **ラズパイ** | 手のひらサイズの小型コンピュータ | 小さな PC |
| **サーバー** | 誰かのリクエストに答えるコンピュータ | お店の店員さん |
| **ブラウザ** | Chrome や Safari。Webページを見るソフト | お客さん |
| **ドメイン** | `example.com` のような人間向けの住所 | お店の名前 |
| **IP アドレス** | コンピュータ同士が使う数字の住所 | お店の実際の住所（番地） |
| **Nginx** | Web サーバーソフト | 店員さんの仕事をするソフト |
| **Cloudflare** | インターネットのインフラ会社 | 警備員 + 受付嬢 |
| **Cloudflare Tunnel** | ラズパイと Cloudflare をつなぐ通路 | 裏口の専用通路 |

---

### インターネットの仕組み

#### ブラウザが Web ページを開く時に起きていること

```
  あなたの PC
      |
      +--①「example.com の IP は何番？」--->  DNS（電話帳サービス）
      |                                              |
      +<--②「93.184.216.34 だよ」------------------+
      |
      +--③ 93.184.216.34 にアクセス！--->  サーバー（93.184.216.34）
      |                                              |
      +<--④ ページのデータを返す-------------------+
      |
      v
  Web ページが表示される！
```

**DNS（Domain Name System）**：人間が読めるドメイン名 → コンピュータが使う IP アドレス に変換する仕組み。電話帳と同じ。

#### HTTP と HTTPS の違い

| プロトコル | 暗号化 | 安全性 |
|-----------|--------|--------|
| `http://` | なし | 盗聴できてしまう |
| `https://` | あり（SSL/TLS） | 安全・今の標準 |

---

### Nginx（エンジンエックス）の役割

```
  ブラウザ
      |
      +--「ページください」--->  Nginx（ラズパイ内の Web サーバー）
      |                               |
      |                               +--->  /var/www/html/index.html を読む
      |                               |
      +<--ページのデータを返す---------+
      |
      v
  ページが表示される
```

Nginx を入れると「サーバーとしての仕事」ができるようになる。  
リクエストを受け取り → ファイルを探して → データを返す。

---

### なぜ家のラズパイに外からアクセスできないのか

#### 家のネットワーク構造

```
  インターネット
       |
       v
  ルーター（外向き IP アドレスを 1 つだけ持つ）
       |
       +----------------------+
       |                      |
       v                      v
  ラズパイ                スマホ / PC
  ( 192.168.x.x )        ( 192.168.x.x )

  <- 家の中だけで有効なプライベート IP アドレス ->
  <- 外のインターネットからは直接届かない       ->
```

`192.168.x.x` は**プライベート IP アドレス**。外のインターネットからは直接届かない。  
→ これが「外からアクセスできない」理由。

#### 普通の解決策（ポートフォワーディング）の問題点

- セキュリティリスクが高い（家の IP が外部に丸見え）
- プロバイダによっては使えない
- 設定が難しい

---

### Cloudflare Tunnel が解決する仕組み

#### 通常 vs Cloudflare Tunnel の経路比較

```
  【通常のポート開放（今回は使わない）】

  外部ユーザー --> インターネット --> ルーター --X--> ラズパイ
                                          ^
                                     ここで止まる

  ----------------------------------------------------------

  【Cloudflare Tunnel（今回の方法）】

  ポイント: ラズパイ側からトンネルを開始する！

  Step 1.  ラズパイ ─────────────────────────> Cloudflare
                    トンネルを張る（常時接続）

  Step 2.  外部ユーザー ─────────────────────> Cloudflare
                         ドメインにアクセス

  Step 3.  Cloudflare ─── トンネル経由 ──────> ラズパイの Nginx
                          リクエストを転送
```

**ポイント：ラズパイ側から接続を開始しているのでルーターに穴を開けなくていい！**

#### Cloudflare が盾になってくれること

```
  [ブロック] 悪意のあるアクセス / DDoS 攻撃 / 不正リクエスト
                       X  <-- Cloudflare がここで防ぐ

  [通過]    正常なアクセス
                       |
                       v
              +--------------------+
              |    Cloudflare      |  ---> ラズパイ（安全！）
              | . DDoS 対策        |
              | . HTTPS 自動適用   |
              | . フィルタリング   |
              +--------------------+
```

---

### 全体の通信フロー（完成後）

```
  ① ブラウザに practice.circlemanage.jp と入力
     |
     v
  ② DNS が「Cloudflare の IP アドレス」を返す
     |
     v
  ③ Cloudflare のサーバーにリクエストが届く
     |
     v
  ④ Cloudflare が事前に張られたトンネルを通じてラズパイに転送
     |
     v
  ⑤ ラズパイの cloudflared が Nginx にリクエストを渡す
     |
     v
  ⑥ Nginx が Web ページのデータを返す（逆の経路でユーザーへ）
     |
     v
  Web ページが表示される！
```

---

### 各手順の意味まとめ

| 手順 | やったこと | 例え |
|------|-----------|------|
| ドメイン取得 | `practice.circlemanage.jp` を買う | お店の名前を決める |
| ラズパイ設定 | Ubuntu を入れ、固定 IP を設定 | お店の場所を固定する |
| Nginx インストール | Web サーバーソフトを入れる | 店員さんを雇う |
| Cloudflare 設定 | DNS 管理を Cloudflare に移す | 警備会社と契約する |
| Cloudflare Tunnel | ラズパイ → Cloudflare の専用通路を開通 | 裏口の専用通路を作る |

---

### 関係する学問・技術分野

| 技術 | 分野 |
|------|------|
| Linux コマンド（`sudo apt install` など） | オペレーティングシステム（OS） |
| IP アドレス・DNS・HTTP | コンピュータネットワーク |
| Nginx の設定 | Web エンジニアリング |
| HTTPS・Cloudflare・ファイアウォール | 情報セキュリティ |
| Cloudflare のようなサービス活用 | クラウドコンピューティング |
